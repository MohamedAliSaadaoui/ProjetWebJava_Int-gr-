parameters:

services:
    _defaults:
        autowire: true
        autoconfigure: true

    App\:
        resource: '../src/'
        exclude:
            - '../src/DependencyInjection/'
            - '../src/Entity/'
            - '../src/Kernel.php'

    App\Enum\RoleEnum: ~

    # Enregistrement du type ENUM personnalisé pour Doctrine

    # Service pour login success handler
    App\Security\CustomLoginSuccessHandler:
        arguments:
            $router: '@router'

    # OAuth service (existant)
    HWI\Bundle\OAuthBundle\OAuth\OAuthUtils: '@hwi_oauth.oauth_utils'
