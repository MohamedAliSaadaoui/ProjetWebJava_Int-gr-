{% extends 'base.html.twig' %}

{% block title %}Create Participation{% endblock %}

{% block body %}
    <div class="container">
    <h1 class="text-center my-4">Participate in Event</h1>

    {{ form_start(form) }}
        <div class="form-group">
            {{ form_label(form.date_participation, 'Date of Participation', {'label_attr': {'class': 'col-sm-2 control-label'}}) }}
            <div class="col-sm-10">
                {{ form_widget(form.date_participation, {'attr': {'class': 'form-control'}}) }}
                {{ form_errors(form.date_participation) }}
            </div>
        </div>

        {# Décommente cette ligne si tu veux inclure le champ pour le nombre de places #}
        {# <div class="form-group">
            {{ form_label(form.nbr_place, 'Number of Places', {'label_attr': {'class': 'col-sm-2 control-label'}}) }}
            <div class="col-sm-10">
                {{ form_widget(form.nbr_place, {'attr': {'class': 'form-control'}}) }}
                {{ form_errors(form.nbr_place) }}
            </div>
        </div> #}

        <div class="form-group">
            <div class="col-sm-offset-2 col-sm-10">
                <button type="submit" class="btn">Participate</button>
            </div>
        </div>

    {{ form_end(form) }}
</div>
{% endblock %}