{% extends 'base.html.twig' %}

{% block title %}Edit Participation{% endblock %}

{% block body %}
    <div class="container">
        <h1 class="text-center my-4" style="color: #4B5320;">Edit Participation</h1>

        {{ form_start(form) }}
            <div class="form-group">
                {{ form_label(form.date_participation, 'Date of Participation', {'label_attr': {'class': 'col-sm-2 control-label', 'style': 'color: #4B5320;'}}) }}
                <div class="col-sm-10">
                    {{ form_widget(form.date_participation, {'attr': {'class': 'form-control', 'style': 'background-color: #f0e1c1; border-color: #4B5320;'}}) }}
                    {{ form_errors(form.date_participation) }}
                </div>
            </div>

            {# Décommente cette ligne si tu veux inclure le champ pour le nombre de places #}
            {# <div class="form-group">
                {{ form_label(form.nbr_place, 'Number of Places', {'label_attr': {'class': 'col-sm-2 control-label', 'style': 'color: #4B5320;'}}) }}
                <div class="col-sm-10">
                    {{ form_widget(form.nbr_place, {'attr': {'class': 'form-control', 'style': 'background-color: #f0e1c1; border-color: #4B5320;'}}) }}
                    {{ form_errors(form.nbr_place) }}
                </div>
            </div> #}

            <div class="form-group">
                <div class="col-sm-offset-2 col-sm-10">
                    <button type="submit" class="btn" style="background-color: #4B5320; color: white;">Edit</button>
                </div>
            </div>

        {{ form_end(form) }}
    </div>
{% endblock %}
