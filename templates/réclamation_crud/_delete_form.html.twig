<form method="post" action="{{ path('app_r_clamation_crud_delete', {'id': r_clamation.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ r_clamation.id) }}">
    <button class="btn">Delete</button>
</form>
