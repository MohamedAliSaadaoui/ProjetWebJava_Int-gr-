<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <title>Connexion - ReWear</title>
    <link rel="stylesheet" href="{{ asset('assets/front/css/bootstrap.min.css') }}">
    <style>
        body {
            background-color: #f8f9fa;
        }
        .login-container {
            min-height: 100vh;
        }
        .card-login {
            max-width: 450px;
            width: 100%;
            border: none;
            border-radius: 12px;
            box-shadow: 0 8px 24px rgba(0, 0, 0, 0.1);
        }
        .brand-header {
            background-color: #4b6f63;
            color: white;
            padding: 2rem;
            border-top-left-radius: 12px;
            border-top-right-radius: 12px;
        }
        .btn-primary-custom {
            background-color: #4b6f63;
            color: white;
        }
        .btn-outline-secondary-custom {
            border-color: #c1b59f;
            color: #6c757d;
        }
        .btn-outline-secondary-custom:hover {
            background-color: #c1b59f;
            color: white;
        }
        footer {
            background-color: #4b6f63;
            color: white;
        }
    </style>
</head>
<body>
    <div class="container d-flex justify-content-center align-items-center login-container">
        <div class="card card-login">
            <div class="brand-header text-center">
                <h2>Connexion à ReWear</h2>
                <p class="mb-0">Entrez vos identifiants pour accéder à votre compte</p>
            </div>
            <div class="card-body px-4 py-4">
                {% if error is defined and error %}
                    <div class="alert alert-danger">
                        {{ error.messageKey|trans(error.messageData, 'security') }}
                    </div>
                {% endif %}
                <form action="{{ path('app_login') }}" method="post">
                    <div class="mb-3">
                        <label for="username" class="form-label">Adresse e-mail</label>
                        <input type="email" id="username" name="_username" class="form-control" required value="{{ last_username }}">
                    </div>
                    <div class="mb-3">
                        <label for="password" class="form-label">Mot de passe</label>
                        <input type="password" id="password" name="password" class="form-control" required>
                    </div>
                    <div class="mb-3 form-check">
                        <input type="checkbox" class="form-check-input" id="remember_me" name="_remember_me">
                        <label class="form-check-label" for="remember_me">Se souvenir de moi</label>
                    </div>
                    <div class="d-grid mb-2">
                        <button type="submit" class="btn btn-primary-custom">Se connecter</button>
                    </div>
                </form>
                <div class="text-center mb-3">
                    <a href="{{ path('app_reset_password') }}">Mot de passe oublié ?</a>
                </div>
                <div class="d-flex justify-content-center align-items-center mb-3">
                    <hr class="flex-grow-1">
                    <span class="mx-2">OU</span>
                    <hr class="flex-grow-1">
                </div>
                <div class="d-grid gap-2">
                    <a href="{{ path('connect_google') }}" class="btn btn-outline-secondary-custom">Google</a>
                    <a href="{{ path('app_user_new') }}" class="btn btn-outline-secondary-custom">Créer un compte</a>
                </div>
            </div>
        </div>
    </div>

    <footer class="text-center p-3 mt-4">
        <p>&copy; 2025 ReWear. Tous droits réservés.</p>
    </footer>

    <script src="{{ asset('assets/front/js/bootstrap.bundle.min.js') }}"></script>
</body>
</html>
